export default function (context) {
  //    const endpoints = context.$auth.strategies.oidc;
   var host = context.req.headers.host;
   console.log('HOST: ' + host)

  //    for (const key in endpoints) {
  //      console.log(key);
  //    }
  }